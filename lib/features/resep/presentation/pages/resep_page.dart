import 'package:flutter/material.dart';

class ResepPage extends StatelessWidget {
  const ResepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Resep Page'),
      ),
    );
  }
}